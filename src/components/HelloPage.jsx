import { useSelector } from "react-redux";

const HelloPage = () => {
 

  return (
    <div>

        <div>
          <h1>Welcome,</h1>
          <p>Your role:</p>
        </div>
        <div>Please log in</div>
    </div>
  );
};

export default HelloPage;
